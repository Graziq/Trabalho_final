name: Run Local Python Code

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run_code: # Nome do job
    runs-on: windows-latest

    # Configuração de serviços de banco de dados PostgreSQL
    services:
      postgres:
        image: postgres:13.11
        env: # Variáveis de ambiente para o contêiner do PostgreSQL
          POSTGRES_DB: postgres-1
          POSTGRES_USER: prefect
          POSTGRES_PASSWORD: prefect
        ports:
          - 5432:5432 # Mapeia a porta do contêiner para a porta do host (opcional, mas boa prática)
        options: >- # Opções extras para o comando 'docker run'
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps: # Sequência de passos
      - name: 🔁 Checkout do código # Passo para baixar seu código
        uses: actions/checkout@v3

      - name: 🐍 Instalar Python 3.10 # Passo para configurar o ambiente Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 📦 Instalar dependências # Instala as bibliotecas Python necessárias
        run: |
          python -m pip install -U pip
          python -m pip install -r requirements.txt
          # Certifique-se de instalar o driver do banco de dados, por exemplo, psycopg2 para PostgreSQL
          python -m pip install psycopg2-binary
          # Remova 'prefect==2.16.4' se você não o usa mais no seu código Python
          # ou se ele não é uma dependência direta do código que você quer rodar localmente.
          # python -m pip install "prefect==2.16.4" 
          
          # Configura PYTHONPATH para Windows (PowerShell)
          # Isso é crucial para que os módulos Python em 'src' sejam encontrados.
          $env:PYTHONPATH="$env:PYTHONPATH;$(Get-Location)\src"
        shell: powershell

      - name: 🏃 Executar Código Python Localmente # Executa seu script Python
        env: # Variáveis de ambiente para o script se conectar ao banco de dados
          PGHOST: postgres
          PGPORT: 5432
          PGUSER: test_user
          PGPASSWORD: test_password
          PGDATABASE: test_db
        run: |
          Write-Host "Tentando executar o código Python localmente..."
          Write-Host "Variáveis de ambiente do BD configuradas para o script."
          
          python src/flows/resultados2.py
          
          # Captura o código de saída do script Python.
          $scriptExitCode = $LASTEXITCODE
          
          if ($scriptExitCode -eq 0) {
            Write-Host "Execução do código Python bem-sucedida!"
          } else {
            # Se o script não executou com sucesso, levanta um erro no workflow.
            throw "A execução do código Python falhou com código de saída: $scriptExitCode"
          }
        shell: powershell
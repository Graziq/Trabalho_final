name: Deploy Prefect Flows

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - name: 🔁 Checkout do código
        uses: actions/checkout@v3

      - name: 🐍 Instalar Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 📦 Instalar dependências
        run: |
          python -m pip install -U pip
          python -m pip install -r requirements.txt
          python -m pip install "prefect==2.16.4"
          
          # Configura PYTHONPATH para Windows (PowerShell)
          # Isso é crucial para que o Prefect encontre os módulos dentro de 'src'
          $env:PYTHONPATH="$env:PYTHONPATH;$(Get-Location)\src"
        shell: powershell

      - name: 🔗 Configurar conexão com Prefect Server remoto
        run: |
          # Define a URL da API do seu Prefect Server usando um secret do GitHub
          prefect config set PREFECT_API_URL="${{ secrets.PREFECT_SERVER_URL }}/api"
          Write-Host "URL da API do Prefect configurada."
          prefect profile ls # Opcional: Para verificar o perfil ativo
        shell: powershell

      - name: 🚀 Deploy dos Flows (Usando -n, conforme sua indicação)
        run: |
          $max_retries = 5
          $retry_delay_seconds = 5
          
          # Lista dos nomes das deployments a serem atualizadas.
          # A sintaxe 'prefect deploy -n <nome_da_deployment>' implica que o Prefect
          # conseguirá encontrar o flow correspondente no ambiente atual (diretórios, PYTHONPATH).
          # Isso é o que você indicou que funciona para você.
          $deployments_to_update = @(
              "simulacao-e-visualizacao-orchestrator-deployment",
              "simulacao-contingencia-flow/deploy-flow"
          )

          foreach ($deployment_name in $deployments_to_update) {
            $attempt = 0
            $success = $false
            Write-Host "Iniciando deploy para a deployment: $($deployment_name)..."

            while ($attempt -lt $max_retries -and -not $success) {
              $attempt++
              Write-Host "Tentativa $($attempt) de $($max_retries) para o deploy de $($deployment_name)..."
              
              # Executa o deploy usando a sintaxe -n
              # Este comando atualizará a deployment existente com o código mais recente
              # presumindo que o Prefect possa encontrar o flow no PYTHONPATH configurado.
              prefect deploy -n "$($deployment_name)"
              
              if ($LASTEXITCODE -eq 0) {
                Write-Host "✅ Deploy de $($deployment_name) realizado com sucesso!"
                $success = $true
              } else {
                Write-Host "❌ Deploy de $($deployment_name) falhou. Aguardando $($retry_delay_seconds) segundos antes de tentar novamente..."
                Write-Host "Erro detalhado (código de saída): $LASTEXITCODE"
                Start-Sleep -Seconds $retry_delay_seconds
              }
            }

            if (-not $success) {
              Write-Error "O deploy da deployment $($deployment_name) falhou após $($max_retries) tentativas."
              exit 1 # Força a falha do passo no GitHub Actios
            }
          }

        shell: powershell
name: Deploy Prefect Flows # Nome do workflow, visível no GitHub Actions

on:
  push: # Este workflow será acionado quando houver um push
    branches:
      - main # Apenas na branch 'main'

jobs:
  deploy: # Definição do job 'deploy'
    runs-on: windows-latest # O job será executado em um runner Windows (latest version)

    steps: # Sequência de passos a serem executados
      - name: 🔁 Checkout do código # Nome do passo
        uses: actions/checkout@v3 # Utiliza a action oficial para fazer o checkout do seu repositório

      - name: 🐍 Instalar Python 3.10 # Nome do passo
        uses: actions/setup-python@v4 # Utiliza a action oficial para configurar o Python
        with:
          python-version: "3.10" # Especifica a versão do Python a ser instalada

      - name: 📦 Instalar dependências # Nome do passo
        # O comando 'run' executa comandos de shell
        run: |
          python -m pip install -U pip # Atualiza o pip para a versão mais recente
          python -m pip install -r requirements.txt # Instala as dependências listadas no requirements.txt
          python -m pip install "prefect==2.16.4" # Instala uma versão específica do Prefect
          
          # Configura PYTHONPATH para Windows (PowerShell)
          # Isso é crucial para que o Prefect encontre os módulos dentro de 'src'
          # Adiciona o diretório 'src' ao PYTHONPATH, garantindo que módulos Python dentro dele sejam encontrados.
          $env:PYTHONPATH="$env:PYTHONPATH;$(Get-Location)\src"
        shell: powershell # Define o shell para este passo como PowerShell

      - name: 🔗 Configurar conexão com Prefect Server remoto # Nome do passo
        run: |
          # Define a URL da API do seu Prefect Server usando um secret do GitHub
          # O secret `PREFECT_SERVER_URL` deve ser configurado nas configurações do seu repositório no GitHub.
          # Isso garante que a URL do servidor Prefect não seja exposta no seu código.
          prefect config set PREFECT_API_URL="${{ secrets.PREFECT_SERVER_URL }}/api"
          Write-Host "URL da API do Prefect configurada." # Exibe uma mensagem no log do GitHub Actions
          prefect profile ls # Opcional: Para verificar o perfil ativo do Prefect (útil para depuração)
        shell: powershell # Define o shell para este passo como PowerShell

      - name: 🚀 Deploy do Flow com retry # Nome do passo
        # **CORREÇÃO AQUI:** Sintaxe do loop for para PowerShell
        run: |
          # Loop for em PowerShell: para tentar o deploy 5 vezes
          # Se o deploy for bem-sucedido, o loop pode ser quebrado para evitar retries desnecessários.
          for ($i = 1; $i -le 5; $i++) {
            Write-Host "Tentativa $i de 5 para deploy do flow..."
            # Execute o comando de deploy do Prefect.
            # Certifique-se de que 'simulacao-e-visualizacao-orchestrator-deployment'
            # aponta para um arquivo de configuração `prefect.yaml` ou similar
            # que está acessível no diretório de trabalho.
            prefect deploy -n simulacao-e-visualizacao-orchestrator-deployment
            
            # Adiciona uma pausa entre as tentativas (opcional, mas recomendado para retries)
            Start-Sleep -Seconds 5 # Pausa por 5 segundos
          }
        shell: powershell # Define o shell para este passo como PowerShell
